{
  "name": "secrets",
  "version": "0.0.1",
  "description": "Configuration encryption/decryption",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "config",
    "encryption",
    "secrets"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.12",
    "timer-profiler": "^0.0.7"
  },
  "dependencies": {
    "cache-via-mongo": "^0.0"
  }
}

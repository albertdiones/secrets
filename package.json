{
  "name": "secrets",
  "version": "0.0.1",
  "description": "Configuration encryption/decryption for added layer of configuration protection",
  "main": "index.ts",
  "scripts": {
    "loop-test": "while true; do bun test --timeout=20000; sleep 5; done",
    "loop-validate": "while true; do bun test --timeout=20000; tsc --noEmit --pretty --skipLibCheck ./index.ts; sleep 5; done"
  },
  "keywords": [
    "config",
    "encryption",
    "secrets"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.12",
    "jest": "^29.7.0",
    "timer-profiler": "^0.0.7"
  },
  "dependencies": {
    "cache-via-redis": "^0.0"
  }
}
